fastapi~=0.86.0
pydantic~=1.10.2
uvicorn~=0.15.0
numpy~=1.22.4
pandas~=1.3.5
scikit-learn~=1.3.0
# Capped below 4.x: anyio 4.x ships a pytest plugin that requires pytest>=7,
# which is incompatible with the pinned pytest~=6.2.5 in requirements-test.txt.
# starlette 0.20.4 (via fastapi 0.86) requires anyio>=3.4.0, so this stays in range.
anyio>=3.4,<4
